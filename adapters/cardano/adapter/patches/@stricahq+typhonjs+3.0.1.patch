diff --git a/node_modules/@stricahq/typhonjs/dist/transaction/Transaction.js b/node_modules/@stricahq/typhonjs/dist/transaction/Transaction.js
index 400acd6..7fedb85 100644
--- a/node_modules/@stricahq/typhonjs/dist/transaction/Transaction.js
+++ b/node_modules/@stricahq/typhonjs/dist/transaction/Transaction.js
@@ -253,9 +253,8 @@ class Transaction {
         const uOutput = output;
         uOutput.tokens = (0, helpers_1.sortTokens)(uOutput.tokens);
         this.outputs.push(uOutput);
-        if (uOutput.plutusData) {
-            this.plutusDataList.push(uOutput.plutusData);
-        }
+        // Note: plutusData on outputs is encoded as inline datum (datum_option [1, tag24(data)])
+        // and must NOT be added to the witness set's plutus_data list (Babbage/Conway era).
     }
     addWithdrawal(withdrawal) {
         if (withdrawal.rewardAccount.stakeCredential.type === types_1.HashType.ADDRESS) {
diff --git a/node_modules/@stricahq/typhonjs/dist/types.js b/node_modules/@stricahq/typhonjs/dist/types.js
index a510035..8b8e479 100644
--- a/node_modules/@stricahq/typhonjs/dist/types.js
+++ b/node_modules/@stricahq/typhonjs/dist/types.js
@@ -62,6 +62,7 @@ var WitnessType;
     WitnessType[WitnessType["PLUTUS_DATA"] = 4] = "PLUTUS_DATA";
     WitnessType[WitnessType["REDEEMER"] = 5] = "REDEEMER";
     WitnessType[WitnessType["PLUTUS_SCRIPT_V2"] = 6] = "PLUTUS_SCRIPT_V2";
+    WitnessType[WitnessType["PLUTUS_SCRIPT_V3"] = 7] = "PLUTUS_SCRIPT_V3";
 })(WitnessType || (exports.WitnessType = WitnessType = {}));
 var NetworkId;
 (function (NetworkId) {
diff --git a/node_modules/@stricahq/typhonjs/dist/utils/encoder.js b/node_modules/@stricahq/typhonjs/dist/utils/encoder.js
index 353cd98..d489632 100644
--- a/node_modules/@stricahq/typhonjs/dist/utils/encoder.js
+++ b/node_modules/@stricahq/typhonjs/dist/utils/encoder.js
@@ -410,6 +410,7 @@ const encodeWitnesses = (vKeyWitness, inputs, plutusDataList, plutusScriptMap, n
     }
     const encodedPlutusScriptsV1 = [];
     const encodedPlutusScriptsV2 = [];
+    const encodedPlutusScriptsV3 = [];
     for (const [script, scriptType] of plutusScriptMap) {
         if (scriptType === types_1.PlutusScriptType.PlutusScriptV1) {
             const pls = cbors.Decoder.decode(buffer_1.Buffer.from(script, "hex"));
@@ -419,6 +420,10 @@ const encodeWitnesses = (vKeyWitness, inputs, plutusDataList, plutusScriptMap, n
             const pls = cbors.Decoder.decode(buffer_1.Buffer.from(script, "hex"));
             encodedPlutusScriptsV2.push(pls.value);
         }
+        else if (scriptType === types_1.PlutusScriptType.PlutusScriptV3) {
+            const pls = cbors.Decoder.decode(buffer_1.Buffer.from(script, "hex"));
+            encodedPlutusScriptsV3.push(pls.value);
+        }
         else {
             throw new Error("Unsupported PlutusScript Version");
         }
@@ -429,6 +434,9 @@ const encodeWitnesses = (vKeyWitness, inputs, plutusDataList, plutusScriptMap, n
     if (encodedPlutusScriptsV2.length) {
         encodedWitnesses.set(types_1.WitnessType.PLUTUS_SCRIPT_V2, encodedPlutusScriptsV2);
     }
+    if (encodedPlutusScriptsV3.length) {
+        encodedWitnesses.set(types_1.WitnessType.PLUTUS_SCRIPT_V3, encodedPlutusScriptsV3);
+    }
     const encodedNativeScriptMap = new Map();
     for (const ns of nativeScripts) {
         const encodedNativeScript = (0, exports.encodeNativeScript)(ns);
@@ -501,9 +509,12 @@ const encodePlutusData = (plutusData) => {
         return [];
     }
     if (plutusData instanceof Uint8Array) {
-        return buffer_1.Buffer.from(plutusData);
+        const buf = buffer_1.Buffer.from(plutusData);
+        if (buf.length > 64) return new cbors.IndefiniteByteString(buf);
+        return buf;
     }
     if (plutusData instanceof buffer_1.Buffer) {
+        if (plutusData.length > 64) return new cbors.IndefiniteByteString(plutusData);
         return plutusData;
     }
     if (typeof plutusData === "string") {
